<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <title>Tính năng đang được phát triển</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap CSS (nếu project bạn đã load rồi thì có thể bỏ) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: #f5f7fb;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
        }
        .dev-card {
            max-width: 600px;
            background: #ffffff;
            border-radius: 16px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.08);
            padding: 32px 28px;
            text-align: center;
        }
        .dev-icon {
            font-size: 3rem;
            margin-bottom: 16px;
        }
        .dev-title {
            font-size: 1.6rem;
            font-weight: 700;
            margin-bottom: 8px;
        }
        .dev-text {
            color: #6c757d;
            margin-bottom: 24px;
        }
    </style>
</head>
<body>

<div class="dev-card">
    <div class="dev-icon">🚧</div>
    <h1 class="dev-title">Tính năng đang được phát triển</h1>
    <p class="dev-text">
        Chức năng bạn truy cập hiện chưa sẵn sàng sử dụng.<br>
        Chúng tôi đang hoàn thiện và sẽ cập nhật trong thời gian sớm nhất.
    </p>

    <a href="/" class="btn btn-primary">
        <i class="bi bi-house-door me-1"></i> Quay lại trang chủ
    </a>
</div>

<!-- Bootstrap JS (nếu cần) -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
